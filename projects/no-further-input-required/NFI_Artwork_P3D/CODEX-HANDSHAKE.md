# CODEX HANDSHAKE — No Further Input Required / Web R4 Presentation Master

Date: 2026-08-25
Project: 《无需进一步输入 / No Further Input Required》
Repository: `ewanqian/portfolio`
Primary runtime: `projects/no-further-input-required/NFI_Web_R4/`
Public entry: `nfi-r4.html`
Reference runtime: `projects/no-further-input-required/NFI_Artwork_P3D/`
Status: **R4 FROZEN ARTWORK / WEB QA PENDING**

## 0. First handshake

Do not redesign, add a visual family or return to PDE as the default editing surface.

Run this sequence first:

1. Pull latest `main`.
2. Read `NFI_Artwork_P3D/CURRENT-EXHIBITION-BOARD.md`.
3. Read `NFI_Artwork_P3D/FINAL-FREEZE-2026-08-25.md`.
4. Open `nfi-r4.html` in the browser.
5. Run one complete 30-second loop unchanged.
6. Only if Web parity is unclear, compare against the frozen Processing R4 implementation.
7. Report:

```text
HANDSHAKE OK
Current master: WEB R4 FROZEN
Browser/runtime: ...
Resolution tested: ...
Observed FPS: ...
30s seam: PASS / FAIL / NOT TESTED
Distance-view hierarchy: PASS / FAIL / NOT TESTED
Web ↔ PDE parity concern: NONE / ...
First proposed tuning: ...
Blockers: NONE / ...
```

## 1. Source-of-truth precedence

```text
1. CURRENT-EXHIBITION-BOARD.md
2. FINAL-FREEZE-2026-08-25.md
3. NFI_Web_R4/sketch.js
4. this handshake
5. frozen PDE implementation as reference
6. research documents
7. archive
```

## 2. Frozen artwork system

```text
Constraint Mechanism
+ Index Field
+ Quantized Memory
+ Sparse Historical Topology
+ Deterministic Outline Glyph Language
```

Shared hierarchy:

```text
12 × 7
→ 24 × 14
→ 72 × 42
```

30-second deterministic loop. No audience input, visible UI, hard scene cuts, particle filler, random glitch, decorative bloom or performance-controller interface.

## 3. Web-first rule

The artistic logic uses deterministic 2D geometry and time/state envelopes. No Processing-specific feature is required for the frozen artwork.

Therefore:

```text
Web = presentation / review / recording master
PDE = frozen reference / provenance
```

Do not maintain two independently evolving versions.

## 4. Ponytail ladder

Before writing code:

```text
1. Does this need changing?
2. Can one existing parameter solve it?
3. Can one existing method solve it?
4. Can something be deleted?
5. Only then add minimum code.
```

## 5. Allowed final tuning

```text
line weight
alpha / brightness
event timing
memory density
topology visibility
glyph scale / density
browser performance
```

Do not add another primitive family or scene without explicitly reopening the artistic freeze.

## 6. Required evidence

```text
browser run
29.5s → 0s seam
stable 60 fps at presentation resolution
4K/fullscreen recording test
10-minute stability
distance-view hierarchy
recording compression
```

The executor’s job is now browser QA and narrow visual tuning, not invention.
