# CURRENT EXHIBITION BOARD — No Further Input Required

Date: 2026-08-25
Status: **R3 CURRENT WORKING MASTER**
Engine: Processing 4 / Java / P3D
Issue anchor: `#56`

This file is the canonical map for the current exhibition build. Start here before reading older project documents.

## 1. Current delivery model

```text
GENERATIVE RUNTIME
30-second deterministic seamless loop
3840 × 2160 target
60 fps target
no audience input
no visible UI

DELIVERY RECORDING
approximately 3 minutes
= six complete 30-second loops
```

The 30-second runtime supersedes the older 180-second runtime for the current submission build.

## 2. Current visual system

Foreground source structures:

```text
Constraint Mechanism
+ Index Field
+ Quantized Memory
```

All three share one grid hierarchy:

```text
12 × 7 major grid
→ 24 × 14 index grid
→ 72 × 42 memory micro-grid
```

Current progression is continuous and overlapping:

```text
registration / sparse address
→ absorption / structural growth
→ recall / historical return
→ autonomy / remainder
→ seamless return
```

No hard scene switch is part of the current artwork.

## 3. Active Processing files

These are the active sketch/runtime files in the root of `NFI_Artwork_P3D/`:

```text
NFI_Artwork_P3D.pde         main runtime / frame clock / keys / review mode
AppConfig.pde               4K, FPS, loop, grid and round configuration
ArtworkContext.pde          loop phase + shared dramaturgical variables
Exhibition30Composer.pde    current R1/R2/R3 exhibition composition
UnifiedGridSystem.pde       Constraint + Index + Quantized Memory grid language
MechanicalGlyphLanguage.pde ASCII / Unicode mechanical mark layer
RUN.md                      local run + QA instructions
CODEX-HANDSHAKE.md          local Codex startup contract
EXHIBITION-30S-R3-BRIEF.md  current artistic/technical brief
```

Anything under `archive/` is historical and should not be treated as active Processing code.

## 4. Current research inside this exhibition board

```text
research/GLYPH-ASCII-NOTHING-RESEARCH.md
research/DESIGN-AESTHETIC-CROSS-REFERENCE.md
```

The first tracks the non-verbal glyph language and Nothing-inspired address/persistence logic.
The second links earlier repository research that can inform the artwork’s visual order, timing, accumulation and recurrence without importing old interfaces wholesale.

## 5. Archive structure

```text
archive/
  2026-08-16-six-state/
    historical six-state Processing source
    historical front-fidelity reports

  2026-08-25-180s-v1/
    superseded 180-second exhibition execution brief
```

The archive exists to preserve provenance while keeping obsolete `.pde` tabs out of Processing’s active sketch compilation.

## 6. Current round ladder

### R1 — ORDER

Constraint + Index + Quantized Memory aligned to one hierarchical grid. No glyph layer.

### R2 — MECHANICAL LANGUAGE

R1 plus restricted ASCII / Unicode marks, addressable zones, progress and persistent anchors.

### R3 — EXHIBITION CANDIDATE

Reduced grid scaffolding, stronger memory/recall, lower glyph density, history that may arrive slightly before current addressing. This is the current baseline for local refinement.

### R4–R10

Do not add a feature backlog. Refine taste and runtime evidence:

```text
line hierarchy
empty/dense proportion
glyph selection/density
pulse timing
recall rarity/history lead
brightness falloff
30-second seam
4K stability
recording compression
distance-view order
```

## 7. Locked visual rules

```text
grayscale / near-black
no readable prose
no state-name display
no HUD/dashboard
no random glitch
no particle filler
no decorative bloom
no automatic camera orbit
no hard scene switches
no literal audio-reactive mapping
no preference-learning claims
```

ASCII/Unicode are mechanical marks, not text content.
Nothing Glyph is used only as a behavioural reference for finite addressable zones, timed patterns, progress, persistence and recall.

## 8. Current acceptance questions

Judge the work before reading an artist statement:

1. Can an earlier structure be recognised when it returns?
2. Does later structure visibly depend on prior history?
3. Is the densest moment ordered rather than merely busy?
4. Is empty space active and intentional?
5. When new activity decreases, does the system remain alive?
6. Does the 30-second boundary feel like continuation rather than restart?
7. From several metres away, is the hierarchy still legible?
8. Do glyphs feel like a machine’s material vocabulary rather than interface copy?

## 9. Runtime truth status

Prepared in repository:

- 4K target configuration;
- 60 fps target;
- deterministic 1800-frame loop;
- R1/R2/R3 selectable comparison;
- 1080p review mode;
- local debug toggle;
- PNG capture.

Still requires local evidence:

- actual compile/run on current machine after this reorganisation;
- measured 3840×2160 FPS;
- 10-minute stability run;
- Unicode fallback/tofu check;
- 29.5s → 0s seam observation;
- final 3-minute screen recording.

Never convert these untested items into completed claims.

## 10. Related repository research

See `research/DESIGN-AESTHETIC-CROSS-REFERENCE.md` for the curated set. The strongest currently indexed sources are:

```text
research/audio-visual-grammar-engine/816-web-to-processing/
research/performance-control-model/DAILY-PRACTICE.md
research/performance-control-model/progress/2026-08-24-round02.md
research/performance-control-model/GRAPHICAL-SPATIAL-CONTROL-LANGUAGE.md
```

These are references, not dependencies. The exhibition artwork stays autonomous and non-interactive.
