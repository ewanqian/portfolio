# Performance Control Model — Daily Progress

**Date:** 2026-08-24  
**Rule:** one active research task / max two hygiene tasks

## ACTIVE RESEARCH TASK

### P0 — Prove quantized audiovisual capsules on one authoritative Web Audio transport

Goal:

Replace the current low-level `key → note/effect` interaction with a small set of short, musically coherent, quantized audiovisual capsules.

Minimum proof:

```text
4–8 capsules
+
one Web Audio transport
+
beat/bar quantization
+
attack / development / decay
+
shared event data for audio + visual
```

Success condition:

A user can trigger the capsules imperfectly in time, but the result still forms a musically stable 30–60 second performance fragment.

This task belongs to **R1 Runtime & Temporal Control**.

---

## WHY THIS IS ACTIVE NOW

Recent observations converge on the same problem:

1. Touch-oriented audiovisual instruments work well when each input recalls a prepared, coherent musical/visual behaviour rather than an isolated note.
2. Multi-sequencer live performance creates long-form structure by activating, removing and modulating already-prepared layers.
3. `No Further Input Required v0.7` already has a continuous musical ground, shared timing and sparse manual intervention, but its discrete keyboard events remain too low-level.
4. WebXR spatial control will only become artistically meaningful when it controls a musically reliable runtime underneath it.

Therefore the immediate dependency is musical/time structure, not additional input complexity.

---

## TODAY'S RESEARCH OUTPUT

Created canonical documents:

- `RESEARCH-MAP.md` — consolidates current research into R1/R2/R3 and defines attention-management rules.
- `RUNTIME-SPEC.md` — defines the shared browser performance runtime, transport, quantization, sequencer, layers, state and input adapters.

Current canonical hierarchy:

```text
ASSET
↓
CAPSULE / CELL
↓
CLIP
↓
SEQUENCE
↓
LAYER
↓
STATE
↓
SHOW
```

Current priority:

```text
P0  Runtime / quantization / capsules / sequencer
P1  WebXR spatial control adapter
P2  control/electronics fundamentals when needed
P3  extended large-visual / lighting / show-control integration
```

---

## HYGIENE TASK 1

Keep `No Further Input Required v0.7` preserved as a historical playable reference.

Do not overwrite the reference while extracting reusable runtime ideas from it.

## HYGIENE TASK 2

Keep Spatial Playground status language accurate:

```text
SPECIFIED / IMPLEMENTED
≠
DEVICE TESTED / PASSED
```

Do not let WebXR harness work claim artistic success before the runtime and real-device validation exist.

---

## PARKING LOT

These ideas are retained but are **not active tasks today**:

- 8-layer sequencer implementation after capsule proof;
- AI-generated structural clips / event JSON;
- AudioWorklet timing optimisation;
- Vision Pro D01 Control Volumes;
- WebSocket → OSC/MIDI stage bridge;
- PWM / duty-cycle experiments as a teaching bridge from button to continuous control;
- electronics fundamentals: voltage/current/resistance, sampling, smoothing, hysteresis;
- control-theory translation into music performance;
- large-format visual / mapping / lighting output;
- raw note: `豆瓣的大视觉 / 大视觉方向` — retain until its specific reference is clarified.

---

## NEXT GATE

Do not start the full 8-layer system until the capsule proof answers:

1. Can one transport schedule all capsules reliably?
2. Does quantization make imperfect input musically acceptable?
3. Does each capsule have a legible beginning, development and release?
4. Can audio and visual share the same event structure without becoming trivial 1:1 reactivity?
5. Can density remain bounded after repeated triggering?

If these pass, unlock:

```text
P0-3 — 8-Layer Sequencer
```

---

# ROUND 01 — 11:56

## ACTIVE RESEARCH TASK

**Unchanged:** prove quantized audiovisual capsules on one authoritative Web Audio transport.

No additional research task is opened in this round.

## DECISIONS LOCKED

### 1. Browser runtime is the shared core

```text
Web Audio
+
Three.js / WebGL
+
WebSocket
```

WebXR, MIDI, OSC and external show-control connections are adapters around the same runtime. They do not define a second musical engine.

### 2. Runtime hierarchy is fixed for the next implementation round

```text
ASSET
↓
CAPSULE / CELL
↓
CLIP
↓
SEQUENCE
↓
LAYER
↓
STATE
↓
SHOW
```

The immediate implementation target stops at `CAPSULE + TRANSPORT + QUANTIZATION`; the later levels stay specified but inactive.

### 3. Input semantics move upward

Do not optimise the existing model as:

```text
key → better note / better effect
```

Move toward:

```text
physical input
↓
semantic action
↓
prepared behaviour / state change
```

Keyboard, touch, MIDI and WebXR should eventually be interchangeable input adapters for the same actions.

### 4. Spatial input remains P1

The WebXR Sensor Scope / Control Volume work remains valid, but additional spatial-control complexity is intentionally paused until the musical runtime can produce a stable performance fragment.

### 5. Touch:waves is a precedent, not a dependency

Use it as a comparative case for prepared audiovisual behaviours, quantization and browser-native interaction. Do not make the research dependent on copying an undocumented or unlicensed production codebase.

## EVIDENCE / RESULT

This round produced a stable implementation boundary:

```text
CURRENT PROBLEM
26 low-level events are not enough

NEXT PROOF
4–8 coherent capsules
+ authoritative clock
+ quantization
+ shared event structure

LATER
8-layer sequencer
+ states
+ WebXR / OSC / MIDI adapters
```

The main result is therefore **scope reduction**, not feature accumulation.

Implementation status at Round 01:

```text
RESEARCH MAP          DONE
RUNTIME SPEC          DONE
ACTIVE TASK           DEFINED
CAPSULE PROTOTYPE     NOT YET IMPLEMENTED
DEVICE VALIDATION     NOT APPLICABLE YET
```

## HYGIENE CHECK

1. Preserve NFI v0.7 as reference; do not mutate it into the runtime during this proof.
2. Keep `IMPLEMENTED`, `TESTED` and `PASSED` as separate status words in all following updates.

## NEXT ACTION

Build the smallest transport/capsule proof with only:

```text
1 AudioContext transport
4 capsules minimum
beat + bar boundary
pending trigger queue
shared audio/visual event timestamp
density / voice ceiling
reset
```

Do not add the 8-layer UI, WebXR controls, AI generation or OSC bridge in the next coding pass unless the capsule proof already works.

## PARKING LOT DELTA

No parked research item was promoted in Round 01.
